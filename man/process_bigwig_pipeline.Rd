% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PeakRankeR.R
\name{process_bigwig_pipeline}
\alias{process_bigwig_pipeline}
\title{Process BigWig Signal Pipeline to Rank Peaks and Extract Top Regions}
\usage{
process_bigwig_pipeline(
  bw_table,
  regions_df,
  summary_type = "mean",
  n_top_values = 500,
  make_ucsc_url = TRUE,
  base_url = NULL,
  weights = c(1, 1)
)
}
\arguments{
\item{bw_table}{A data.frame or tibble containing BigWig file paths and associated metadata.}

\item{regions_df}{A data.frame of genomic regions to summarize (must include chr, start, end, Peak_ID).}

\item{summary_type}{Character string specifying the summary statistic to use for BigWig signal aggregation (default: "mean").}

\item{n_top_values}{Integer specifying the number of top peaks to extract per sample (default: 500).}

\item{make_ucsc_url}{Logical indicating whether to add UCSC Genome Browser URL links to the output (default: TRUE).}

\item{base_url}{Character string of the base URL for UCSC Genome Browser links (no default; required if \code{make_ucsc_url = TRUE}).}

\item{weights}{Numeric vector of length 2 to weight coverage and magnitude ranks respectively (default: c(1, 1)).}
}
\value{
A data.frame with top ranked peaks per sample, including columns: chr, start, end, Peak_ID, subclass (sample),
and optionally UCSC_URL if \code{make_ucsc_url = TRUE}.
}
\description{
This function runs a multi-step pipeline to summarize BigWig signal over genomic regions,
calculate peak coverage proportion and magnitude ranks, combine these ranks (optionally weighted),
and then extract the top N ranked peaks per sample. Optionally, it adds UCSC Genome Browser URLs for visualization.
}
