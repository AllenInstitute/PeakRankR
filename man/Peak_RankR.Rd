% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Peak_RankR.R
\name{Peak_RankR}
\alias{Peak_RankR}
\title{Rank Enhancer Peaks Across Cell Groups}
\usage{
Peak_RankR(
  tsv_file_df,
  group_by_column_name = "cell_type",
  background_group = NULL,
  bw_table,
  rank_sum = TRUE,
  weights = c(1, 1, 1),
  chr_col = "chr",
  start_col = "start",
  end_col = "end",
  magnitude_col = NULL,
  verbose = TRUE
)
}
\arguments{
\item{tsv_file_df}{A data frame of peaks containing chromosome, start, end,
magnitude, and group columns.}
\item{group_by_column_name}{Character. Name of the group column (e.g.
\code{"cell_type"}, \code{"subclass"}). Default: \code{"cell_type"}.}
\item{background_group}{Character vector of groups used as specificity
background. If \code{NULL}, all groups in the data are used.}
\item{bw_table}{A data frame with columns \code{file_path} and
\code{sample_id}.}
\item{rank_sum}{Logical. If \code{TRUE}, rank by sum of per-metric ranks.
If \code{FALSE}, rank by composite score. Default: \code{TRUE}.}
\item{weights}{Numeric vector of length 3: weights for specificity,
sensitivity, magnitude. Default: \code{c(1, 1, 1)}.}
\item{chr_col}{Column name for chromosome. Default: \code{"chr"}.}
\item{start_col}{Column name for start coordinate. Default: \code{"start"}.}
\item{end_col}{Column name for end coordinate. Default: \code{"end"}.}
\item{magnitude_col}{Column name for signal magnitude in \code{tsv_file_df}.
If \code{NULL} (default), magnitude is computed automatically from the
mean bigwig signal of the target group at each peak.}
\item{verbose}{Logical. Print progress messages. Default: \code{TRUE}.}
}
\value{
The input data frame with five new columns appended:
\code{specificity_score}, \code{sensitivity_score} (1 = peak active in fewest groups), \code{magnitude_score},
\code{PeakRankR_score}, and \code{PeakRankR_rank} (1 = best).
}
\description{
Scores and ranks genomic enhancer peaks by specificity, sensitivity, and
magnitude of bigwig signal across user-defined cell groups.
}
\examples{
\dontrun{
tsv_file <- read.table(
  system.file("extdata", "test_file.tsv", package = "PeakRankR"),
  header = TRUE, sep = "\t"
)
bw_table <- read.table(
  system.file("extdata", "bw_table.txt", package = "PeakRankR"),
  header = TRUE, sep = "\t"
)
ranked <- Peak_RankR(
  tsv_file_df          = tsv_file,
  group_by_column_name = "cell.population",
  background_group     = unique(tsv_file$cell.population),
  bw_table             = bw_table
)
}
}
